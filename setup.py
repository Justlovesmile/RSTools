from setuptools import find_packages, setup

setup(
    name = 'RSTools',
    version = '0.1.0',
    description = 'Tools for remote sensing images (RSIs) processing and visualization',
    author = 'Justlovesmile',
    packages=find_packages()
)